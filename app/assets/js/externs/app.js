/*
 * List global variables here. Google Closure Compiler will keep them intact.
 */

// Google Analytics counter
var GoogleAnalyticsObject = 'ga';
function ga() {}
function gaEvent(category, action, label) {}
