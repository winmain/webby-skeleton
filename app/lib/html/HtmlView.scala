package lib.html

import webby.html.{HtmlBuffer, StdHtmlView}

abstract class HtmlView(buf: HtmlBuffer = new HtmlBuffer) extends StdHtmlView(buf) {

  // ------------------------------- Tags -------------------------------

  // TODO: place your custom tags here
}
